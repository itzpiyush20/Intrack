# AGENTS.md

**Canonical agent instructions live in [`CLAUDE.md`](CLAUDE.md). Read that file.**

This file exists only so tools that look for `AGENTS.md` find a pointer. It is
deliberately a stub: two full copies of the same instructions drifted apart once
already (one said the next migration was `042_` while the real one was `048_`),
so there is now exactly one source.

Also worth reading before any change:

- [`ARCHITECTURE.md`](ARCHITECTURE.md) — what the repo actually contains: every
  serverless handler, service module, route and database table, and how the
  Gmail scanner runs.
- [`README.md`](README.md) — setup and deployment.
- [`PRODUCT.md`](PRODUCT.md) / [`DESIGN.md`](DESIGN.md) — audience, brand, and the
  visual system.
